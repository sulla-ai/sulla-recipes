
services:
  twenty-crm-server:
    image: twentycrm/twenty:latest
    container_name: twenty-crm-server
    ports:
      - "30207:3000"
    volumes:
      - twenty-crm-server-data:/app/packages/twenty-server/.local-storage
    environment:
      NODE_PORT: 3000
      SERVER_URL: "http://localhost:30207"
      PG_DATABASE_URL: postgres://twenty:{{sullaServicePassword|urlencode}}@twenty-crm-postgres:5432/twenty
      REDIS_URL: redis://host.docker.internal:30117
      APP_SECRET: "{{sullaN8nEncryptionKey}}"
      STORAGE_TYPE: local
    depends_on:
      twenty-crm-postgres:
        condition: service_healthy
    restart: unless-stopped

  twenty-crm-worker:
    image: twentycrm/twenty:latest
    container_name: twenty-crm-worker
    command: ["yarn", "worker:prod"]
    volumes:
      - twenty-crm-server-data:/app/packages/twenty-server/.local-storage
    environment:
      PG_DATABASE_URL: postgres://twenty:{{sullaServicePassword|urlencode}}@twenty-crm-postgres:5432/twenty
      REDIS_URL: redis://host.docker.internal:30117
      APP_SECRET: "{{sullaN8nEncryptionKey}}"
      STORAGE_TYPE: local
    depends_on:
      twenty-crm-server:
        condition: service_healthy
    restart: unless-stopped

  twenty-crm-postgres:
    image: postgres:16-alpine
    container_name: twenty-crm-postgres
    ports:
      - "30208:5432"
    environment:
      POSTGRES_DB: twenty
      POSTGRES_USER: twenty
      POSTGRES_PASSWORD: {{sullaServicePassword}}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - twenty-crm-postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U twenty"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  twenty-crm-server-data:
  twenty-crm-postgres-data:
