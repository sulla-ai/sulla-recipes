id: twenty
name: Twenty CRM
description: Beautiful modern open-source CRM — pipelines, activities, companies, people, calendar, and more (private Salesforce alternative)
icon: twenty.svg
version: "2026.02"
category: crm

compose:
  composeFile: "docker-compose.yml"

setup: []

commands:
  start: "docker compose -f ${COMPOSE_FILE} up -d"
  stop: "docker compose -f ${COMPOSE_FILE} down"
  restart: "docker compose -f ${COMPOSE_FILE} restart"
  status: "docker compose -f ${COMPOSE_FILE} ps"
  update: "docker compose -f ${COMPOSE_FILE} pull && docker compose -f ${COMPOSE_FILE} up -d"
  logs: "docker compose -f ${COMPOSE_FILE} logs --tail=100"

openInBrowser: true

extraUrls:
  - label: "Twenty CRM"
    url: "http://localhost:30207"
