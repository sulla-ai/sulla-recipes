id: appflowy
name: AppFlowy
description: Open-source Notion alternative — beautiful workspace with databases, documents, AI, and real-time collaboration
icon: appflowy.svg
version: "2026.02"
category: productivity

compose:
  composeFile: "docker-compose.yml"

setup:
  - type: shell
    command: "docker compose build --build-arg uid=$(id -u) --build-arg gid=$(id -g)"
    optional: false

commands:
  start: "docker compose up -d"
  stop: "docker compose down"
  restart: "docker compose restart"
  status: "docker compose ps"
  update: "docker compose pull && docker compose up -d --build"
  logs: "docker compose logs --tail=100"

openInBrowser: true

extraUrls:
  - label: "AppFlowy Workspace"
    url: "http://localhost:30260"
